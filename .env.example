# Database connection (SQLite for local development, Postgres-compatible for production)
DATABASE_URL="file:./dev.db"

# JWT Secret for Session Authentication
JWT_SECRET="tripshield_super_secret_jwt_key_enterprise_2026"

# Optional: Anthropic Claude API Key for Stage B natural-language explanations
# If left empty, TripShield automatically uses its deterministic rule-engine ranking + templated rationale.
ANTHROPIC_API_KEY=""
